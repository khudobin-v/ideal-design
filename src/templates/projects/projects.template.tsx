import { ProjectsBlock } from "@/components";

export const Projects = () => {
  return (
    <div
      id="projects"
      className="flex h-screen items-center justify-center bg-black"
    >
      <ProjectsBlock />
    </div>
  );
};
